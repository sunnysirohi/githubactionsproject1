on:
  push

jobs:
  display_output:
    name: Run a simple commmand
    runs-on: ubuntu-latest
    steps:
      - name: Display the output
        run: |
          echo "This is my first workflow"
        shell: bash
